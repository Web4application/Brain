# NeoMind Learning Engine

This folder implements an incremental live-learning service for Brain.

## Features
- Incremental training on new data
- REST API: /upload-data, /train, /predict, /metrics
- Scheduler for automatic learning cycles
- Dockerized for easy deployment

## Run locally
```bash
python app_incremental.py
